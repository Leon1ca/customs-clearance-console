# v1.6.0 本地发行物

本机正式发行物位于：

- 当前可运行目录：`artifacts/local-current`
- 完整压缩包：`artifacts/releases/v1.6.0/关单核验台-Windows-x64-v1.6.0.zip`

压缩包 SHA-256：

`FA5AD849ED21C67475868030039F0E760D15FD9B4003EB5962E967701E289193`

验证结果：根目录启动器文件版本为 `1.6.0.0`，UI 契约自检返回 `UI_CONTRACTS_OK`。

`artifacts` 不提交到 Git。GitHub Release 负责长期保存正式二进制，Git 标签负责保存对应源码状态。
