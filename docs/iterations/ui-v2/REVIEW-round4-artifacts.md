# 第四轮：云端产物独立视觉与导出审查

审查基准：`72212a3a60da497573fda13aa849060a5b27ff0d`，GitHub Actions run `35955897503`。证据目录：`/Users/leon1ca/Documents/Codex/2026-09-24/evidence-72212a3`。代码定位均来自 `git show 72212a3:…`，不代表正在修复的工作区。未在本机运行产品、编译或测试；仅查看 PNG、读取 JSON、静态解析 XLSX ZIP/XML 和 Markdown。未修改产品文件或旧 preflight 文档。

结论：本提交不能通过 UI 验收。空白文字并非都可归因于 DrawToBitmap：至少按钮标题、批次状态、KPI 数据及布局有确定代码根因。浏览器 E2E 本轮失败，不在本报告中重复 R2 清单。

## 必须修复

### R4-1 / P1：设置及翻页按钮确实没有文字

- 产物：`ui-states/dialog-settings.png` 的两个“浏览”、取消、保存按钮均为空白；主窗口上一页/下一页为空白。
- 根因：`Theme.cs:122–129` 的按钮工厂接收 `text` 却不使用，`Style` 在 `145–150` 固定 `Text = string.Empty`。`DirectorySettingsForm.cs:79–85,117–121` 和 `MainForm.Layout.cs:221–228` 直接调用这些工厂，没有二次赋值。使用 `IconButton` 的开始识别/导出则能显示，因为它在 `Theme.cs:133` 再赋值。
- 修复：所有工厂正确传递文字，并为可操作按钮设置可访问名称。云端验证设置的浏览、取消、保存以及前后翻页文字非空且在实际可见矩形内；截取可见窗口复核。

### R4-2 / P1：工具栏和页脚子控件高度溢出，文字落到父容器外

- 所有主窗口 PNG 的筛选文字、搜索图标/占位文字、清理文字、页脚均缺失或裁切。不能只换截图方式后忽略此问题。
- 独立几何证据：`ui-states.json` 的真实菜单记录是 `menu-cleanup` anchor `{X=891,Y=376,Width=112,Height=100}`；本应容纳它的工具栏行只有 56 高。菜单被锚定在 y=482，远离工具栏。该尺寸是运行时 `RectangleToScreen(button.ClientRectangle)`，不是位图绘制造成的。
- 代码：`MainForm.Layout.cs:163–175` 的外层记录行固定 56，但内层单行 toolbar 未配置 RowStyle；`211–239` 页脚亦未配置 RowStyle。`SearchField` 继承 Panel 的默认尺寸且仅 Dock 填充；它的搜索图标按 `UiControls.cs:208` 的 `Height / 2` 居中。默认最小内容高度使嵌套自动行超出可见槽位，符合产物中只剩按钮上沿的形态。
- 修复：为这些嵌套单行 TableLayoutPanel 显式设置可收缩的行布局，并合理设置子控件高度/最小尺寸与搜索框内边距。云端记录 toolbar/footer 与全部子控件的实际 Bounds，断言子控件可见区域及文字中心都位于父级 ClientRectangle；清理锚点高度应匹配实际按钮设计高度。再用屏幕抓图确认筛选四段、搜索、清理及分页完整可读。搜索占位文字是否还受 DrawToBitmap 影响须由真实抓屏确认，不能仅靠当前位图下结论。

### R4-3 / P1：空批次、待识别及识别中 KPI 重叠裁切，文件数也错误

- 产物：`ready-1440x900.png` 正文“已载入 10 个文件”，左上 KPI 却“本批文件 0 份”。`unloaded-1200x720.png`、`ready-1440x900.png`、`processing-1440x900.png` 的注释压在标签/数字上，第二排数字被卡片底边切掉。
- 根因：`MainForm.cs:442` 永远使用 `records.Count`，待识别并无记录，应使用已经载入的 `_batchFileCount`。`477–478` 及 `MainForm.Layout.cs:349–350` 将无金额行的摘要压到 126 高；`UiV2Controls.cs:41–79` 此时走非 compact 布局，每格仅约 62 高，14 顶内边距后标签20、数值从+22开始高38，而注释位于 cell.Bottom−20。绘制矩形必然交叠，第二排数值越界，与截图一致。
- 修复：摘要高度至少容纳 KPI 的两行完整内容，或为矮卡片定义真正不同布局；不要仅按币种数决定 KPI 高度。待识别显示载入文件数，处理中区分总文件数与已识别数，未载入按设计显示占位状态。云端分别验证 0 文件、10 文件未识别、6/10 处理中、单币种完成及多币种完成。

### R4-4 / P2：标题区状态和批次信息没有接线

- 对照设计 `01-main.png`、`07-ready-not-recognized.png`、`08-processing.png`，产物所有状态只有“本批关单”，没有状态徽标及路径/时间等副标题。
- 根因：`UiV2Controls.cs:560–563` 的 `_status`、`_subline` 初始为空；`572–579` 虽提供 `Set`，但本提交全仓库 `_titleBlock` 仅有 `MainForm.cs:31` 声明和 `MainForm.Layout.cs:129–130` 创建/添加，没有调用 Set。
- 修复：将真实批次状态、目录/载入信息、识别完成信息传入标题组件，并随状态转换更新；补四状态视觉证据。这里是明确的未实现，不是字体或截图问题。

### R4-5 / P1（验收阻断）：所谓四档窗口截图实际都被云端桌面限制

- 静态读取 PNG IHDR：所有名为 `*-1200x720.png` 的主窗口是 **1044×720**；1280×800、1440×900、1920×1080 三档全部是 **1044×788**。没有一张主窗口达到声明尺寸。
- `ui-states.json` 还记录实际工作区 `{Width=1024,Height=720}`。环境实际 DPI 为 96，因此不能解释为 125/150/200% 缩放。
- `SelfTest.cs:664–682` 在 Show 前设 Size，Show 后仅当 DPI≠96 时重设 ClientSize，未对最终尺寸断言。`579–580` 用请求 width/height 计算 Responsive 报告，未读实际 ClientSize，所以报告会宣称使用 480/560 KPI 宽度而实际一直在最窄布局。
- 修复：云端准备足够大的实际桌面/虚拟显示，再设置窗口最终 ClientSize 并断言。报告同时保存请求逻辑尺寸、实际 ClientSize/Bounds、PNG 像素尺寸、屏幕工作区、实测 DPI、实际 responsive 分支。无法满足时失败或明确未覆盖，不能输出“四档通过”。
- 现有 RenderForm 使用 `DrawToBitmap`，菜单也先 DrawToBitmap 再合成（`SelfTest.cs:685–691,724–731`）。应至少对关键状态另提供可见窗口的真实屏幕截图，以区分 HWND 子控件打印差异和真实 UI。尺寸正确后重新比较原稿；当前不能判断 1440/1920 的列宽、密度和响应式是否合格。

### R4-6 / P1：Excel 保留数值但显示仍会把小数量变成零

- 直接读取实际导出 `关单列表_合成样例.xlsx`：关单明细 `G12` 数值为 `0.0004`、style=5；styles.xml 对应格式 `#,##0.###`。Excel 正常显示将为 `0`。同一行 `I12=0.1234567` 使用 style=6、`#,##0.00####`，显示只能保留六位小数。
- 定位：`ExcelListExporter.cs:319–320,339–340`。现有回读验证只核对底层数字，无法发现显示值误导。Markdown 对应数据完整保留 `0.0004 KG` 和 `0.1234567`，因此两种导出视觉口径不一致。
- 修复：数量/单价采用足够精度或依据原始有效位数的显示格式，保留数值类型；云端验收需同时检查数值和格式后的可见结果，尤其非零小数不能显示为零。

## 导出静态核对结果与仍有限制

- XLSX 三表实际为“关单列表”61 行、“关单明细”60 行、“币种汇总”3 行（均不含表头）；Markdown 有61个关单段落，包含第二页以外记录及重复记录。
- 报关单号 `010120260000000001` 和合同 `000123` 为 inlineStr，前导零保留；`=SUM(A1:A2)`、`+1-2` 是文本；全部 worksheet 内公式节点数为0。金额为数值，缺失项未用0补齐。复核单位及差异列存在。
- 根据列表各币种数值列独立用 Decimal 求和，与汇总表一致：CNY 663203.55→663203.55，EUR 650886.65→650886.65，USD 662250.70→659670.70；重复扣减2580.00对应非代表记录。
- Markdown 的管道符、尖括号、反斜杠及 `&` 已转义；小数量/单价未丢精度。未在 Office 中打开，不能宣称 Excel 视觉排版已验收。
- 旧 preflight 的“整项一致误判”在本产物仍可见：明细第2/3/4行复核字段均为空却 Q 列为“一致”，包括缺失商品/数量/单价的第4行。代码仍为 `ExcelListExporter.cs:422` 仅凭 `!HasSecondaryDifference` 写“一致”。这属于已经交给修复者的既有问题，不应把本轮 `export-validation.json:Pass=true` 当作关闭证据。
- 部分合成样例的 Note 保留“主4800”而实际主金额为6195.45等，说明 fixture 文案未随数值更新；不据此判定导出程序篡改，但修复后应让样例自洽，避免错误证据掩盖业务语义。

## 字体、DPI、详情与打包证据

- 环境 JSON 实测 Noto Sans SC Regular/Bold/Medium、JetBrains Mono Regular/Medium 均可加载，静态字体和 OFL 资产存在。当前主文字和详情图清楚；这项比之前只有代码声明更充分。
- 当前真实环境只有96 DPI。`realMonitorScalingVerified=false` 是诚实的标注，125/150/200% 实际窗口仍未验证，数学断言不能替代实际截图。
- `dialog-detail.png`、`dialog-detail-conflict.png` 的商品列和分币种合计可读；冲突图仅将可靠 USD 7680.00计入合计，并说明另一项未确认；没有看到无条件同源“一致”误导。此结论仅覆盖这两个样例，未覆盖长名称、多页、多币种滚动及极小数量详情。
- environment 中 `rootLicense=false`、`thirdPartyNotices=false` 是构建目录探测，不能据此认定最终便携包缺文件。本轮浏览器步骤失败，现有证据未提供最终包的完整成功验收；仍需同一最终 SHA 的便携包展开清单、root/tools、LICENSE/第三方声明/字体OFL及从包运行的云端证据。

## 下一次验收应交付

同一提交上修复上述产品缺陷，先让可见布局几何断言失败即停，再提供实际达到声明尺寸的关键状态抓屏；不要只增加字符串/常量自检。浏览器 E2E 和便携包验证必须在该同一 SHA 成功。后续独立复验应以最终产物重新判断关闭状态，本报告不替代对正在修复代码的验收。

生产浏览器兼容边界：主路由提供的公开页面静态核对 `planning/official-query-reference.md` 记录，官方 pi4 使用 `id=myedit` 的跨源 iframe，目标默认主机为 `swapp.singlewindow.cn`、路由 `/qspserver/sw/qsp/query/view/queryDecStatus`，主机可能受运行期配置覆盖。本报告读取了该核对记录，未另行执行真实查询。简单拒绝所有跨源 iframe 可以防止假成功，却不能据此宣告官方截图主功能完成；最终应证明已核实官方 iframe 路径的长内容可完整截取，或者明确保留该生产场景未完成。真人验证码和生产结果仍需如实列为人工验收边界。
